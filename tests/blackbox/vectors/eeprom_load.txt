# EEPROM load configuration
# Verify: loadEEpromSettings applies correct defaults and mappings
# Note: loadEEpromSettings is called indirectly via init(),
# but we can test by setting eeprom fields and checking derived state

@config
armed=1
inputSet=1
dshot=1
EDT_ARMED=1

@sequence
# With default (zeroed) eeprom, unidirectional mode
tick 1  | throttle=1000 | adjusted_input=1000

# Bidirectional changes throttle mapping
config eeprom.bi_direction=1
tick 1  | throttle=1200 |
